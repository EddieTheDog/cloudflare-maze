{
  "name": "cloudflare-maze",
  "version": "1.0.0",
  "description": "Secret redirect maze with Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
